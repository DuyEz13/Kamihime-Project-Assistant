[project]
name = "kamiwiki"
version = "0.1.0"
description = "FastAPI wiki and crawler for Kamihime Project object data"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "beautifulsoup4>=4.12,<5",
    "deepl>=1.22,<2",
    "fastapi>=0.115,<1",
    "httpx>=0.27,<1",
    "jinja2>=3.1,<4",
    "langchain>=1,<2",
    "langchain-deepseek>=1,<2",
    "langchain-google-genai>=4,<5",
    "langchain-openai>=1,<2",
    "langchain-qdrant>=1,<2",
    "langgraph>=1,<2",
    "qdrant-client>=1.15,<2",
    "python-dotenv>=1,<2",
    "uvicorn[standard]>=0.30,<1",
]

[project.optional-dependencies]
translation = [
    "accelerate>=1.2,<2",
    "autoawq==0.2.9; sys_platform == 'linux'",
    "torch==2.6.0",
    "transformers==4.51.3",
]
rag = [
    "fastembed>=0.7,<1",
    "sentence-transformers>=5,<6",
]

[dependency-groups]
dev = [
    "pytest>=8,<9",
]

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
pythonpath = ["."]
